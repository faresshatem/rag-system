global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Placeholder for your Python RAG application
  # You will need to expose a metrics endpoint (e.g. using prometheus_client)
  # - job_name: 'rag_backend'
  #   static_configs:
  #     - targets: ['host.docker.internal:8000']
